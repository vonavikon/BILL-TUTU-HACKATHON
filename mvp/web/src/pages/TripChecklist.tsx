// Устарел: чек-лист заменён прямым чатом с бэкендом (см. TripPlan). Больше нигде
// не роутится; оставлен только чтобы не удалять файл — компилируется в сборке.
export default function TripChecklist() {
  return null;
}
